# ── 服务 ─────────────────────────────────────────────
PORT=8787
JP_HOST=127.0.0.1

# 单次分析的最大字符数（超出部分会被截断并给出警告）
JP_MAX_TEXT=20000

# 词典 zip 存放目录 / 数据库目录（默认为项目下的 dictionaries、data）
# JP_DICT_DIR=/home/your-user/jp-sentence/dictionaries
# JP_DATA_DIR=/home/your-user/jp-sentence/data
